# matcha-finder

Discover local matcha cafés + drinks.

## Database (Docker)

```bash
docker compose up -d
```

Default Postgres credentials:
- Host: `localhost:5432`
- User: `postgres`
- Password: `postgres`
- Database: `matcha_finder`
